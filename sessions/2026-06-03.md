# Session: AGI-OS skeleton + operator + Voicenotes auto-sync
Date: 2026-06-03
Type: work | money
Tags: agi-dashboard, build, agi-os, voicenotes, operator, drive

## Objective
Make the dashboard chat a true operator, get Voicenotes syncing on a schedule, and stand up the
AGI-OS folder system as the durable context layer (Layer 2), ready to back up to Google Drive.

## Context Used
identity/core.md (drafted this session), projects/agi-dashboard/context.md, live Supabase
(schema, mcp_call), OpenClaw workflow doc, project memory.

## Decisions Made
- Chat agent given full-environment tools + skills loop; never claims "no access".
- Voicenotes sync uses the OpenClaw integration REST API (raw key), not MCP-over-SSE.
- Cron writes route through mcp_call (SECURITY DEFINER) to stay RLS-safe under anon.
- Connector layer = Klavis (open-source, self-host for $0 at scale, data sovereign); Hermes/OpenClaw
  connect to the brain directly over MCP, not via a connector platform.
- AGI-OS lives in the dashboard repo and syncs to Google Drive for portable backup.

## Artifacts Created
- AGI-OS/ skeleton (this folder): claude.md, identity/*, routing/*, projects/*, intelligence/*, skills/*, sessions/*.
- Commits: operator (fff9944), intelligence render (85078a6), voicenotes cron (d5aea57).

## What to Remember
The dashboard is now a working single brain: operator chat + scheduled note sync + durable
file context layer. Drive backup is the last piece and needs George's one OAuth click.

## Next Checkpoint
Wire Klavis Google Drive → sync AGI-OS/ up. Then auto-generate session files like this one at
the end of each chat block, and wire Hermes/OpenClaw to /api/mcp.
